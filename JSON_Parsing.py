import streamlit as st

st.set_page_config(
    page_title="JSON Parser",
    page_icon="📟"
)

st.title("Fuse JSON Parsing tools")

st.sidebar.success("Select a parsing tool above.")

st.write("This app helps parse JSON files for use by the team. Select a parsing tool from the menu on the left.")